var Purchase = {
  price: parseInt($('#price-amount').html()) + <%= @product["price"] %>,
  setPrice: function() {
    $("#price-amount").html(Purchase.price)    
  }
}

console.log(Purchase.price);
console.log(Purchase.setPrice());
Purchase.setPrice();