<%= form_for message, remote: true, authenticity_token: true do |f| %>
  <%= f.label :deine_nachricht %>:
  <%= f.text_area :content, class: "form-control", data: {textarea: "message"} %>

  <%= f.hidden_field :chatroom_id, value: variable.chatroom.id %>

  <div class="container container-links no-padding">
  	<% if user_signed_in? %>
  		<%= f.submit "send", class: "btn btn-green btn-lg", id: "messageSubmit", data: {send: "message"} %>
  	<% else %>
  		<%= link_to :signup, new_user_registration_path, class: "btn btn-green btn-lg" %>
  	<% end %>
<% end %>