<% Event::ICONS.each do |key, parameters| %>
	<%= link_to url_path(key, variable), method: parameters.last, class: 'icon-link' do %>
    <%= image_tag parameters.first, class:"icon-s1" %>
  <% end %>
<% end %>
