<h1>Spiel Karte</h1>
<p id="description">Im SprachSpiel gibt es Standorte, wo man kann verschiede Tätigkeiten machen. Jeder Spieler hat eine Rolle und wird Ihren eigenen Raum bauen, so dass jedes einen teil des Spieles verwalten kann..</p>
<p id="description">Klicken Sie auf das Symbol, um das Produkt zu kaufen!</p> 

<%= render partial: "partials/resources" if user_signed_in? %>   
<div class="row">
  <%= render @products %>
</div>

<div class="row" id="items-purchase">
  <p> this is the price</p>
  <div id="price-amount">0</div>
  <%#= render @items if @items.present? %> <!-- if @purchase.items.present? && user_signed_in? -->
</div>